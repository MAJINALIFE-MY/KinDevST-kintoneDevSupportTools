{
  "name": "kindevst",
  "version": "2.0.0",
  "private": true,
  "description": "kintone Developer Support Tool - Chrome extension build tooling",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
